# Cool-ambient target requalification — 2026-09-17

Status: passive target-capture evidence. No application CAN transmissions were generated by the bridge during this capture.

Target installation:

- outdoor: 5TWV0X24A1000B
- indoor: 5TAMXC03AV31DB
- controller: UX360 + SC360 (TLINK360A2VVUG)
- refrigerant: R-454B / A2L
- parsed TRANE_CAN_LIVE frames: 1,902
- unique standard CAN IDs: 83

This capture is valuable because outdoor ambient fell from roughly 93 F in the prior high-load run to roughly 78.5 F while the compressor remained at 55 RPS. Several previously ambiguous channels separate cleanly across those two operating points.

## Stable operating anchors

    0x200 u16[0] = 157..158
    0x200 u16[1] = 815

    0x280 = 58.0 / 2.5
    0x281 = u16 [775, 500, 0, 2116..2117]
    0x300 = 55.23..56.85 F / 47.544 F
    0x308 = 73.991 F / 50.668..50.734 F
    0x310 = 0.1409..0.1448 / -99
    0x318 = 1.033691 A / u16[2] 803..810 / u16[3] 545..547
    0x320 = 80.23..81.21 W

    0x380 = -99 / 78.47..78.63 F
    0x381 = 84.51..84.57 / 167.99..168.22
    0x382 = 65.35..65.57 / 81.60..81.76
    0x383 = 140.973 / 324.0
    0x384 = 57.9..58.1 / u16 343..355 / u16 768..774
    0x385 = 1195..1201 / 69..71
    0x386 = 2.0 / 50.0
    0x387 = 55.0 / 0.4
    0x388 = 4.3 / 4.3
    0x389 = 4.3 / 5.4..5.5
    0x38C = 0 / 1361..1401 W
    0x38F = 238..239 / 3.2..3.3

## Refrigerant block: a correction to the high-load correction

The prior high-load pass correctly disproved 0x383.float[1] as line voltage and 0x38F.float[0] as liquid pressure, but it over-corrected 0x381.

### 0x381.float[0] — suction temperature

Restore the original suction-temperature interpretation.

At this operating point:

    0x381.float[0] ~= 84.5 F
    0x381.float[1] ~= 168.1

The Chemours Opteon XL41 / R-454B pressure-temperature table gives about 168.5 psig saturated-vapor/dew pressure at 65 F. Interpreting 0x381.float[1] directly as suction pressure therefore gives approximately:

    84.5 F suction temperature - ~65 F dew saturation ~= 19.5 F superheat

That is physically coherent.

The earlier hot capture strengthens the same interpretation rather than contradicting it:

    0x381.float[0] ~= 98.7 F
    0x381.float[1] ~= 195.3 psig

R-454B dew pressure is about 183.1 psig at 70 F and 198.7 psig at 75 F, putting 195.3 psig near 74 F. That gives roughly 25 F suction superheat.

Current best mapping:

    0x381.float[0] -> suction temperature
    0x381.float[1] -> suction pressure, psig

This is now supported by two substantially different ambient/load points plus the older Technician vocabulary.

### 0x383.float[1] — liquid/high-side pressure candidate

The current value is exactly 324 psig while the true line-voltage candidate remains about 239 V.

Chemours' R-454B bubble-pressure table gives about:

    100 F -> 300.8 psig
    105 F -> 322.8 psig
    110 F -> 345.9 psig

Thus 324 psig corresponds to about 105 F saturated-liquid temperature.

The leading liquid-line temperature channel is 0x382.float[1] ~= 81.6..81.8 F, giving approximately 23 F of subcooling.

The hotter capture gave roughly 395..400 psig with 0x382.float[1] ~= 96 F. R-454B bubble pressure is about 395.7 psig at 120 F, again giving approximately 24 F subcooling.

That repeatability across two ambient conditions is highly discriminating.

Current best mapping:

    0x383.float[0] -> compressor dome/discharge temperature candidate
    0x383.float[1] -> liquid/high-side pressure candidate, psig
    0x382.float[1] -> liquid-line temperature candidate

### 0x382.float[0] remains unresolved

Do not call this outdoor-coil temperature or liquid temperature yet.

It is roughly 65.4 F in the current 78.5 F ambient run and roughly 69.7 F in the prior 93 F ambient run. An outdoor condenser-coil sensor should not behave that way in normal cooling, and the liquid-line role is already much better supported by 0x382.float[1].

Retain generic temperature-candidate wording until synchronized Technician data identifies it.

### 0x38F.float[0] — line-voltage candidate strengthened

Current capture:

    0x38F.float[0] = 238..239

Prior hot capture:

    0x38F.float[0] ~= 237

Meanwhile 0x383.float[1] moved roughly 324 -> 395..400 with outdoor conditions/load.

This makes 0x38F.float[0] by far the strongest line-voltage candidate. Keep Candidate wording until a synchronized meter/Technician point is recorded.

## 0x385.float[0] — compressor-power candidate strengthened

The old outdoor-EEV label is definitively invalid.

Current:

    0x385.float[0] = 1195..1201
    0x38C.float[1] = 1361..1401 W
    ratio ~= 0.86..0.88

Prior hot capture:

    0x385.float[0] = 1445..1486
    0x38C.float[1] = 1617..1701 W
    ratio ~= 0.87..0.90

The Technician application exposes separate compressor power and total/input power channels, so compressor power remains the leading interpretation for 0x385.float[0].

0x385.float[1] = 69..71 remains a compressor target-speed candidate.

## Indoor blower block: 0x281 tail is not RPM

This capture falsifies the old 0x281.u16[3] = blower RPM promotion.

Prior high-load point:

    0x281 ~= [774, 500, 0, 356]
    blower power ~= 46 W
    static ~= 0.089 inWC

Current point:

    0x281 ~= [775, 500, 0, 2116..2117]
    blower power ~= 81 W
    static ~= 0.143 inWC

Airflow is essentially unchanged while the supposed RPM value changes by almost 6x. It cannot be literal motor RPM.

The tail is better preserved as individual bytes:

    prior: byte6=0x64 (100), byte7=0x01
    current: byte6=0x44/0x45 (68/69), byte7=0x08

This looks like value + state/mode packing rather than a single little-endian speed word.

### 0x318 now holds the leading blower-speed candidate

Current 0x318:

    float0 = 1.033691 A
    u16[2] = 803..810
    u16[3] = 545..547

The older Technician reference showed blower speed near 529 RPM. The current u16[3] value is close to that scale and, within this capture, follows blower power/static materially better than the 0x281 tail.

Current best candidate interpretation:

    0x318.float[0] -> blower input-current candidate
    0x318.u16[2]  -> airflow/requested-airflow family candidate
    0x318.u16[3]  -> blower-speed candidate, rpm

A synchronized airflow sweep is still required before removing Candidate wording from the two trailing words.

## 0x280.float[1] is not a literal power factor

The current run has:

    actual airflow ~= 775 CFM
    blower power ~= 80..81 W
    blower current candidate ~= 1.034 A
    0x280.float[1] = 2.5

Therefore 2.5 is neither a physical power factor nor merely an inactive/shutdown sentinel. The bounded Power-Factor-Like Candidate runtime entity has been removed. Preserve the raw 0x280.float[1] value only.

## Structured JSON: ZoneStatus.H is room temperature

This capture reassembles:

    {"ZoneStatus":{"Update":{"1":{"H":"76.00"}}}}

The Home Assistant room-temperature state at the same time is 76.0 F and the raw 0x490 zone-1 temperature family is 76..77 F.

This upgrades the structured mapping:

    ZoneStatus.Update.1.H -> zone-1 room temperature, F

from merely inherited/upstream behavior to target-observed proof.

The paired 0x641 response is again:

    {"Ack":"200"}

## CANopen SDO transport identified

The same capture upgrades the JSON transport from "companion channel" inference
to a standards-backed decode.

Observed JSON SDO pairs:

    0x601 <-> 0x581
    0x641 <-> 0x5C1
    0x649 <-> 0x5C9

The request-side IDs carry standard CANopen SDO download commands and the
response-side IDs carry the corresponding standard SDO server responses.

All three decoded JSON transfers target manufacturer object:

    index    0x300A
    subindex 0x00

### Block SDO: 0x649 / 0x5C9 ZoneStatus

    0x649 C2 0A 30 00 2E 00 00 00
    0x5C9 A0 0A 30 00 07 00 00 00
    0x649 01 ...
    ...
    0x649 87 ...
    0x5C9 A2 07 00 00 00 00 00 00
    0x649 CD 00 00 00 00 00 00 00
    0x5C9 A1 00 00 00 00 00 00 00

This is standard CANopen block download:

- C2: initiate block download with indicated size;
- 0A 30 00: object 0x300A:00;
- 2E 00 00 00: 46 transferred bytes;
- A0 ... 07: server accepts with block size seven;
- 01..06/87: seven data segments, final bit on sequence seven;
- A2 07: server acknowledges all seven;
- CD: end request with three unused bytes in the final segment;
- A1: successful end response.

The 46 bytes are the 45-byte ZoneStatus JSON plus a trailing NUL.

The same command-byte arithmetic validates the 0x601 Debug messages. A 37-byte
transfer occupies six segments (42-byte capacity), leaving five unused bytes;
the observed end command is D5, exactly C1 | (5 << 2).

### Segmented SDO: 0x641 / 0x5C1 Ack

    0x641 21 0A 30 00 0E 00 00 00
    0x5C1 60 0A 30 00 00 00 00 00
    0x641 00 <7 data bytes>
    0x5C1 20 ...
    0x641 11 <7 data bytes>
    0x5C1 30 ...

This is standard segmented SDO download:

- 21: initiate with a 14-byte indicated size;
- 60: server initiate response;
- 00 / 20: first data segment and matching toggle-0 response;
- 11 / 30: final toggle-1 segment and matching response.

The transferred payload is the NUL-terminated JSON document:

    {"Ack":"200"}

The analyzer now reports these as CANopen SDO events rather than proprietary
transport controls.

### Safety consequence for active writes

The historical repository transmitter was based on the earlier guessed framing.
It did not address object 0x300A:00 and did not wait for the mandatory CANopen
SDO server responses.

That writer is now fail-closed. Passive decode remains unaffected. A future
active writer must be a real asynchronous SDO client, and command direction must
first be qualified against a captured stock UX360 command transaction.


## Raw diagnostic rendering bug found and fixed

The HA raw-frame helper had a 24-byte text buffer but used a conservative pos + 3 < sizeof(bytes) guard. A complete spaced eight-byte frame needs 23 characters plus NUL; after seven bytes the old guard rejected byte eight.

That is why HA showed examples such as:

    Last 0x281 Raw = 07 03 F4 01 00 00 45

while the live frame was:

    07 03 F4 01 00 00 45 08

The buffer is now 3 * 8 + 1 bytes and a regression test locks in complete eight-byte rendering.

## Runtime corrections from this pass

    0x381.float[0]  -> Suction Temperature
    0x381.float[1]  -> Suction Pressure
    0x382.float[0]  -> generic Temperature 1 Candidate
    0x382.float[1]  -> Liquid Temperature Candidate
    0x383.float[1]  -> Liquid Pressure Candidate
    0x385.float[0]  -> Compressor Power Candidate
    0x38F.float[0]  -> Line Voltage Candidate
    0x460.float[0]  -> generic Temperature Candidate

    0x281.tail      -> raw + separate byte6/byte7 candidates
    0x318.u16[2]   -> Airflow Candidate
    0x318.u16[3]   -> Blower Speed Candidate
    0x280.float[1] -> generic raw only; no PF-derived entity

## Next discriminating capture

The single most useful remaining experiment is still a synchronized Technician Monitor capture, but the target list is smaller now:

1. actual airflow, requested airflow and blower RPM while stepping normal airflow through at least three steady points;
2. suction pressure/temp and liquid pressure/temp at the same timestamps;
3. compressor power, total input power and outdoor EEV steps;
4. any Technician field corresponding to the unresolved 0x382.float[0], 0x460.float[0], 0x430, and 0x450 families.

No synthetic leak event is needed for any of these tests.
